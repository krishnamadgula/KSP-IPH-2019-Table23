import catalogueStrings from './catalogue.js'

const allStrings = Object.assign({},
  catalogueStrings
)

const lang = {
  'ar': {
    'language': 'Arabic',
    'strings': allStrings
  }
}
export default lang
